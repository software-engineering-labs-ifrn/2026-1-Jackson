version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: orcamento_mysql
    restart: always
    environment:
      MYSQL_DATABASE: meu_orcamento
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: